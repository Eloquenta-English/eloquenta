# HANDOFF — Eloquenta Library / Agent-Modes Work (2026-07-11)

Session context ID (if relevant): C:\Users\irieb\AppData\Roaming\com.hermes-ide.terminal\context\5fc2a005-ec9f-4ea9-9512-57d5b18d0bf8.md

---

## 1. WHO / WHAT
- User: irieb — ESL/English teacher building teaching products (slide decks, portfolio sites) toward an **EU Edtech grant**.
- Mission: make language education a viable career for professionals from failing industries; employ a caring employer; empower teachers as educators + content creators + polyglots; **AI empowers staff (strict rules), never replaces them**.
- Environment: Windows 10, MSYS bash, Hermes Agent. Brave is the test browser. Live HTTPS host + real-device (phone + PC) testing required (user rule).

## 2. WORK DONE THIS SESSION (verified on disk)

### A. Accent-reduction deck (SHIPPED)
- File: C:\Users\irieb\Documents\William's Projects\websites\eloquenta\Learning Materials\Accent\egyptian-accent-reduction.html
- Single-file HTML deck modeled on japanese-accent-reduction.html. 30 slides. Egyptian Arabic phonology (p/v, th, q→glottal, stress, schwa), psychology-of-L2 section (anxiety, identity, psychological safety, growth mindset), switchgear QC case study with B1+/B2 phrase banks per stage + Q&A phrases, 7-accent speech-synthesis player.
- Verified: 1 `<body>`, 30 slides, JS passes `node --check`.
- Open in Chrome/Edge for the audio player.

### B. Agent-modes pilot (sec-audit) — PROVEN, on disk
- Profiles live at: C:\Users\irieb\AppData\Local\hermes\profiles\ (NOT ~/.hermes/profiles — that path does NOT exist).
- `hermes profile create sec-audit` → made isolated tree (own SOUL.md, skills/, memories/, plans/, cron/, sessions/). Wrapper: `sec-audit` → `hermes -p sec-audit`.
- SOUL.md is POINTER-style: references canonical C:\Users\irieb\.hermes\rules.md, adds 6 sec-audit non-negotiables (no secret requests, redact, scope bounds, evidence, table format, subagent stack). No rules duplication.
- Mode-file: C:\Users\irieb\AppData\Local\hermes\profiles\sec-audit\memories\sec-audit-mode.md — scanner→reporter subagent contract + verification gates.
- Bundle `/sec-audit` created: red-teaming + external-api-config + safe-file-rewrites + dogfood. File: C:\Users\irieb\AppData\Local\hermes\skill-bundles\sec-audit.yaml.
- **KEY VERIFIED FACT:** `hermes profile use <x>` is STICKY (applies to NEXT session, not hot-swap). So the "mode router" is a RECOMMENDER that tells the user to open `hermes -p <mode>` — it cannot switch mid-session. This is the corrected architecture; the old plan assumed hot-swap and was WRONG.
- Scanner→reporter contract proven directly (real issue-vs-fix table, secret redacted, all gates passed). NOTE: automated delegate_task fan-out hit HTTP 401 (subagent API creds not provisioned in delegation runtime) — that's an env gap, not a design flaw. Fix later if automated subagent stacks are wanted.
- Other existing profiles already present: kimi-audio, kimi-decks, kimi-games, kimi-langapps (segmented by MODEL, not task).

### C. Plans written (not executed — design only)
- C:\Users\irieb\.hermes\plans\2026-07-11-mode-aware-profiles.md — full 6-profile mode design (general/web-dev/game-dev/sec-audit/video/esl-teach) + mode-router skill + subagent stacks. Decision gates A/B/C were NEVER answered.
- C:\Users\irieb\.hermes\plans\2026-07-11-product-profiles-workflow-planning-site.md — product profiles + workflow + planning-site.
- C:\Users\irieb\.hermes\plans\2026-07-11-eloquenta-library-vps.md — VPS hosting + tiered monetization + deck editor (THE CURRENT PRIORITY, see §4).

### D. Memory note
- Attempted to save grant-mission + monetization-model to memory but HIT 5000-char cap; remove calls didn't free room (loop risk, stopped). Those facts live in the plan file §4 below instead. Trim older memory entries if you want them persisted.

## 3. DECISIONS STILL OPEN (from eloquenta-library-vps plan)
User wants: VPS-hosted Eloquenta Library; free limited batches (lead magnet) → $5 one-time "full batch" (own-it-offline single-file HTML PWA via Gumroad/Payhip) → editor license (B2B) for teachers who customize decks. Brand = affordable, anti-scalp. Optional annual membership ~€49/yr.
Needs user picks on 4 gates (defaults = a/a/a/a):
1. VPS: (a) write Caddy config + Library page, user provisions server — OR (b) also walk provisioning step-by-step (Hetzner CX22 ~€4-5/mo recommended).
2. Editor v1: (A) lightweight config/JSON editor (fast, reuses VBookMaker data pattern) — RECOMMENDED, OR (B) full WYSIWYG (big, phase 2).
3. Payment: Gumroad/Payhip (zero backend, ~10% cut) — RECOMMENDED, OR self-hosted Stripe.
4. Free batch source: slice existing (IELTS B1 sample + egyptian accent deck) — RECOMMENDED, OR fresh mini content.

## 4. CURRENT PRIORITY = Eloquenta Library (monetization + grant traction)
Plan: C:\Users\irieb\.hermes\plans\2026-07-11-eloquenta-library-vps.md
Tiers: Free batch (€0, funnel) → Full batch ($5 own-it-offline via Gumroad/Payhip) → Editor license (€29-99 B2B) → stretch annual membership (€49/yr).
VPS stack: Linux ~2GB RAM; Caddy reverse proxy (auto Let's Encrypt) serving static decks; each batch = folder with index.html + manifest.webmanifest + sw.js (installable offline PWA); Library page with free link + $5 button + email signup; NO payment backend (Gumroad/Payhip handles it).
Editor requires a DECK SCHEMA first (decks are hand-authored HTML today, not editor-parseable). Recommend lightweight config editor (option A).

## 5. NEXT ACTIONS (in order, verify between)
1. Get user's 4 gate picks (or "defaults").
2. Phase 1: PWA-ify ONE existing deck (add manifest + sw.js). Verify install + offline + progress persists on real device (phone + PC).
3. Write Caddy/VPS config + Library page. User provisions VPS (or we walk it). Verify HTTPS + install.
4. Launch free batch from existing content. Verify downloads.
5. Define deck JSON schema + build lightweight editor (option A). Verify edit→export→install.
6. Editor license flow via Gumroad/Payhip. Verify purchase→access.
7. Compile grant-traction doc: signups, $5 sales, editor licenses, usage.

## 6. CORRECTION — "AGENT MODES MEMORY" = HIVE MEMORY, NOT memory TOOL
User clarified: the per-mode memory system he asked for lives in the **Hive Memory vault** (Obsidian), NOT the agent's internal `memory` tool. The earlier "memory full" issue was a red herring — those facts belong in Hive, not in the 5000-char `memory` store.

### Current Hive Memory status (verified)
- `C:\Users\irieb\Documents\Hive Memory\Agent Rules\` contains ONLY `00 — Mandatory Revision.md` — which is **0 bytes (empty)**.
- NO mode files exist: no web-dev.md, game-dev.md, sec-audit.md, video.md, esl-teach.md, general.md.
- So the Hive mode-file system was **never built** (not unfeasible — just not started).
- DEVIATION to fix: the sec-audit pilot's mode-file was wrongly placed in the profile's PRIVATE memory (`C:\Users\irieb\AppData\Local\hermes\profiles\sec-audit\memories\sec-audit-mode.md`) instead of Hive Memory as originally requested. It must move/copy into Hive.

### NEXT ACTIONS — build the Hive mode-file system (do this)
1. Write 6 mode files into `C:\Users\irieb\Documents\Hive Memory\Agent Rules\`:
   - `general.md` — text-first research, site-analysis pattern, design a11y (80vw, 18px+, masking), honesty/no-fabrication, small-batches.
   - `web-dev.md` — scroll-driven-only, exact px, high-contrast, Pixabay-not-Unsplash, no surname-in-titles, verification checklist.
   - `game-dev.md` — animation critical path (revert-to-milestone), scroll-driven, no time-based, dup cam.lookAt check, matrix-rain globals.
   - `sec-audit.md` — never ask for secrets in chat, secure local input (hermes auth add), red-team scope bounds, issue-vs-fix report format, scanner→reporter stack.
   - `video.md` — no room-codes in video apps, real-device test, cloudflare/WebRTC stack.
   - `esl-teach.md` — B1 13-slide structure, B2 dotted-underline vocab, click-for-def (no L1), match games, masking rule, people-first a11y.
   Each mode-file structure: Role & Objective / Loaded Skills (reminder) / Hard Constraints / Subagent Stack (if any) / Verification gate. Pointer-style — reference canonical rules.md, do NOT duplicate it.
2. Move/copy the existing sec-audit mode-file from the profile's private memory INTO `Hive Memory\Agent Rules\sec-audit.md` (canonical home). Keep profile memory pointing to it or empty.
3. Update each Hermes profile SOUL.md (sec-audit done; others when created) to POINT to its Hive mode file, not embed it — prevents drift.
4. Build the `mode-router` SKILL (in `general` profile, always loaded) that classifies the request → recommends `hermes -p <mode>` (sticky, not hot-swap). Keyword map in plan 2026-07-11-mode-aware-profiles.md.
5. Replicate the sec-audit profile template to the other 5 modes (needs user gate picks A/B/C or defaults a/a/a).

Note: profiles + bundles are the REAL isolation layer (proven). Hive mode files are the SHARED, human-readable memory each mode reads. Both coexist; Hive is the canonical "memory particular to that mode" the user originally asked for.

## 7. HARD RULES TO HONOR (injected every session, but restated)
- No unasked changes; verify before claiming done; revert immediately on request.
- Exact hex colors applied precisely; high contrast.
- ESL decks: topic-relevant backgrounds, no emojis, one theme button, people-first a11y (text ~80vw, body ≥16-18px).
- Text-first research (web_search/web_extract, YouTube transcripts); vision = QA only.
- CSP meta in every HTML after charset.
- Deploy live HTTPS, test on real devices.
- AI empowers, never replaces (grant narrative).
- python -m http.server must use --bind 127.0.0.1 (IPv4 fix on Windows).

## 7. QUICK REFERENCE PATHS
- Accent deck: ...\websites\eloquenta\Learning Materials\Accent\egyptian-accent-reduction.html
- japanese template: ...\Learning Materials\Accent\japanese-accent-reduction.html
- sec-audit profile: C:\Users\irieb\AppData\Local\hermes\profiles\sec-audit\
- Bundles: C:\Users\irieb\AppData\Local\hermes\skill-bundles\
- Plans folder: C:\Users\irieb\.hermes\plans\
- Canonical rules: C:\Users\irieb\.hermes\rules.md
